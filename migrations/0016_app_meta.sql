-- Служебные записи приложения: отпечатки секретов, версия секрета опросов.
--
-- Отпечаток — sha256 от пары «логин:пароль». По нему исходное значение не
-- восстанавливается, поэтому хранить его в базе безопасно. Нужен, чтобы
-- отличить «ADMIN_PASSWORD сменили в окружении» от «ничего не менялось»:
-- во втором случае база авторитетна и ротация через CLI переживает рестарт.
--
-- Таблица служебная, но создаётся в baseline: логика старта на неё
-- рассчитывает с первого же релиза Этапа 3.

-- Up Migration
create table app_meta (
  key        text primary key,
  value      text not null,
  updated_at timestamptz not null default now()
);

-- Down Migration
drop table app_meta;
